# generated by SuperSlicer 2.7.61.8 on 2025-09-01 at 06:17:29 UTC

### 0: 不允许生成空层，确保每一层都有材料挤出。
### 是否允许空层（没有任何挤出动作的层）。0=不允许，1=允许。
allow_empty_layers = 0

### 禁用弧形拟合，打印机将打印所有线段。
### 是否启用G2/G3圆弧拟合以减少G-code体积。disabled=禁用。
arc_fitting = disabled
# Arc fitting 分辨率设置为 100%，在启用时，代表以最高精度拟合弧曲线（当前设置为禁用）。
# 圆弧拟合的精度，100%为默认精度。
arc_fitting_resolution = 100%
# 弧拟合容忍度设置为 5%，定义绘制时允许的误差范围（当前设置为禁用）。
# 圆弧拟合允许的最大误差百分比。误差越大G-code越短，但精度越低。
arc_fitting_tolerance = 5%
# 自动发出温度命令以确保打印头在打印过程中保持正确的温度。
# 自动在G-code中插入温度设置指令（如M104/M140）。1=自动插入。
autoemit_temperature_commands = 1

### 不避免穿越卷曲的悬空部分，可能会影响打印质量。
### 是否尽量避免空行程经过翘起的悬垂部分。0=不避开。
avoid_crossing_curled_overhangs = 0
### 在非第一层时避免穿越其他层，以减少干扰。
### 是否仅在非首层时避免空行程穿越模型。1=只在非首层生效。
avoid_crossing_not_first_layer = 1
# 不避免穿越外周轮廓，可能影响外观。
# 是否尝试让移动路径绕开外圈（外壁）。0=不避开。
avoid_crossing_perimeters = 0
### 最大绕行距离设置为 0，表示不允许绕行。
### 绕开外圈时允许的最大绕行距离。0=不绕行。
avoid_crossing_perimeters_max_detour = 0
### 避免穿越顶部，以减少在顶部层上的干扰。
### 是否尽量避免空行程穿越模型顶部实心区域。1=避开。
avoid_crossing_top = 1

### 打印床的形状坐标，定义了打印床的尺寸和形状。
### 打印床的四个角坐标，矩形床从(0,0)到(200,200)mm。
bed_shape = 0x0,200x0,200x200,0x200
# 打印床温度（一般用于普通层，单位℃）。0=不加热。
bed_temperature = 0
# 每层开始前插入的G-code（为空，表示不添加）。
before_layer_gcode =
# 打印多个物体时，在物体之间切换时插入的G-code（为空）。
between_objects_gcode =
### G-code 输出为二进制格式的设置，0 表示禁用。
binary_gcode = 0

# 底部填充图案，monotonic为单向顺序填充，提升表面均匀度。
bottom_fill_pattern = monotonic

### 底部实心层数（模型底部有3层为全实心）。
bottom_solid_layers = 3
# 底部实心层的最小总厚度。0=只按层数决定。
bottom_solid_min_thickness = 0
# 桥接时的加速度设定。0=默认加速度。
bridge_acceleration = 0
# 桥接填充的角度。0=自动决定。
bridge_angle = 0
# 桥接时风扇转速百分比，100=全速。
bridge_fan_speed = 100
# 桥接区域填充图案，rectilinear=直线交错。
bridge_fill_pattern = rectilinear
# 桥接时的出料比，100%=标准出料。
bridge_flow_ratio = 100%
# 桥接填充与支撑部分的重叠比例，90%。
bridge_overlap = 90%
# 桥接填充与支撑部分的最小重叠比例，80%。
bridge_overlap_min = 80%
# 桥接检测精度参数，影响桥接区域的识别。
bridge_precision = 25%
# 桥接时的打印速度，相对于默认速度的百分比。
bridge_speed = 60%
# 桥接逻辑类型，nozzle=以喷嘴为中心的桥接。
bridge_type = nozzle
# 桥接填充边缘留出的位置（百分比）。
bridged_infill_margin = 200%
# 打印边框(brim)时的加速度。0=默认加速度。
brim_acceleration = 0
# 是否启用brim耳朵，仅在模型拐角处生成局部边框。0=不启用。
brim_ears = 0
# 检测brim耳朵的最小长度。
brim_ears_detection_length = 1
# brim耳朵的最大角度限制。
brim_ears_max_angle = 125
# brim耳朵的填充图案，concentric=同心圆。
brim_ears_pattern = concentric
# 是否在孔洞内部也生成brim。0=不在孔内生成。
brim_inside_holes = 0
# 是否每个物体各自生成brim。0=所有物体共用一个brim。
brim_per_object = 0
# brim与物体本体之间的间隔距离（mm）。
brim_separation = 0
# brim打印速度，占默认速度的百分比。
brim_speed = 50%
# brim的宽度（mm）。0=不生成brim。
brim_width = 0
# 内部brim宽度（mm）。0=无内部brim。
brim_width_interior = 0
# 打印腔体温度（用于封闭式打印机）。0=不加热。
chamber_temperature = 0
# 颜色切换时插入的G-code（为空，表示无自定义操作）。
color_change_gcode =
# 用于多色打印的分层高度（为空，表示无多色分层）。
colorprint_heights =
# 是否启用“逐个完成”模式（每次只完成一个物体）。0=不启用。
complete_objects = 0
# “逐个完成”模式下是否只打一个skirt。0=每物体一个skirt。
complete_objects_one_skirt = 0
# “逐个完成”模式下的打印顺序。object=按物体顺序。
complete_objects_sort = object
# 换料装置冷却管长度（用于多色/多料打印，单位mm）。
cooling_tube_length = 5
# 换料冷却管回抽距离（单位mm）。
cooling_tube_retraction = 91.5
# 凹角平滑的最小角度（0=禁用）。
curve_smoothing_angle_concave = 0
# 凸角平滑的最小角度（0=禁用）。
curve_smoothing_angle_convex = 0
# 曲线平滑的最大作用距离（单位mm）。
curve_smoothing_cutoff_dist = 2
# 曲线平滑的精度参数（0=默认）。
curve_smoothing_precision = 0
# 默认加速度（0=使用固件默认值）。
default_acceleration = 0
# 默认风扇速度，!100 表示100%，感叹号代表可被其他参数覆盖。
default_fan_speed = !100
# 默认打印速度（mm/s）。
default_speed = 100
# 回抽恢复时的速度（0=默认）。
deretract_speed = 0
# 前几层关闭风扇。1=关闭。
disable_fan_first_layers = 1
# 桥接部分不生成支撑。1=不支撑桥接。
dont_support_bridges = 1
# 草图挡风罩（防止风吹翘边），disabled=关闭。
draft_shield = disabled
# 多重打印物体时的最小间距（mm）。
duplicate_distance = 6
# 每次换料结束时执行的G-code。
end_filament_gcode = "; Filament-specific end gcode \n;END gcode for filament\n"
# 打印结束后执行的G-code，包括关闭加热、回零、关闭步进电机。
end_gcode = M104 S0 ; turn off temperature\nG28 X0  ; home X axis\nM84     ; disable motors\n
# 强制填充体积一致，避免局部过少或过多。1=启用。
enforce_full_fill_volume = 1
# 首层也强制使用回抽。1=启用。
enforce_retract_first_layer = 1
# 确保竖直外壳厚度，enabled_old为旧版算法。
ensure_vertical_shell_thickness = enabled_old
# 外部填充与外壁的最小距离（百分比）。
external_infill_margin = 150%
# 外壁加速度（0=默认）。
external_perimeter_acceleration = 0
# 外壁拐角的切角比例（百分比）。
external_perimeter_cut_corners = 0%
# 奇数层外壁是否调整出料量。0=不调整。
external_perimeter_extrusion_change_odd_layers = 0
# 外壁挤出线间距（空=按默认计算）。
external_perimeter_extrusion_spacing =
# 外壁挤出宽度（喷嘴宽度的105%）。
external_perimeter_extrusion_width = 105%
# 外壁打印时风扇速度，!100 表示100%，可被他处覆盖。
external_perimeter_fan_speed = !100
# 外壁与内壁重叠比例。
external_perimeter_overlap = 80%
# 外壁打印速度为默认速度的50%。
external_perimeter_speed = 50%
# 是否先打印外壁。0=按默认顺序。
external_perimeters_first = 0
# 是否强制先打印外壁。0=不强制。
external_perimeters_first_force = 0
# 孔洞部分外壁设置。1=启用。
external_perimeters_hole = 1
# 非孔洞部分外壁设置。1=启用。
external_perimeters_nothole = 1
# 花瓶模式下外壁设置。0=禁用。
external_perimeters_vase = 0
# 装料时额外移动距离（mm，负值为后退）。
extra_loading_move = -2
# 是否插入额外外壁。0=不插入。
extra_perimeters = 0
# 奇数层是否插入额外外壁。0=不插入。
extra_perimeters_odd_layers = 0
# 悬垂处是否插入额外外壁。0=不插入。
extra_perimeters_on_overhangs = 0
# 喷头抬升的安全高度（mm），用于多喷头切换防止撞到模型。
extruder_clearance_height = 20
# 喷头在切换时的安全半径范围（mm）。
extruder_clearance_radius = 20
# 喷头颜色设定（为空，表示默认或未指定）。
extruder_colour = ""
# 挤出机在不同速度下的出料倍增表。格式为“速度:倍增”，用于补偿不同速度下出料误差。
extruder_extrusion_multiplier_speed = 0:10:1:10x1:20x1:30x1:40x1:60x1:80x1:120x1:160x1:240x1:320x1:480x1:640x1:960x1:1280x1
# 喷头风扇偏移量（百分比）。0%表示无偏移。
extruder_fan_offset = 0%
# 多喷头打印时各喷头相对坐标偏移，0x0为无偏移。
extruder_offset = 0x0
# 挤出机温度补偿（℃），0表示不补偿。
extruder_temperature_offset = 0
# 挤出轴在G-code中的标识，一般为E。
extrusion_axis = E
# 全局出料倍增系数，1为标准出料。
extrusion_multiplier = 1
# 挤出线间距（mm），0为默认自动计算。
extrusion_spacing = 0
# 挤出宽度（mm），为空则自动按喷嘴宽度计算。
extrusion_width =
# 单层打印时间低于此秒数时风扇全速（秒）。
fan_below_layer_time = 60
# 风扇启动时的初始高转速时间（秒），0为不用此功能。
fan_kickstart = 0
# 风扇运行的百分比（0表示使用默认设置）。
fan_percentage = 0
# 风扇最低转速（0=不强制最低转速）。
fan_printer_min_speed = 0
# 悬垂处是否提升风扇强度。1=启用。
fan_speedup_overhangs = 1
# 风扇加速达到目标转速所需时间（秒），0为默认。
fan_speedup_time = 0
# 特殊功能G-code（为空）。
feature_gcode =
# 当前耗材的颜色，用于可视化和多色信息。
filament_colour = #29B2B2
# 换料冷却阶段的最终进料速度（mm/s）。
filament_cooling_final_speed = 3.4
# 换料冷却阶段的初始进料速度（mm/s）。
filament_cooling_initial_speed = 2.2
# 换料冷却阶段的移动次数。
filament_cooling_moves = 4
# 换料冷却区暂停时间（秒）。
filament_cooling_zone_pause = 0
# 每千克耗材价格（用于成本估算，0为未设置）。
filament_cost = 0
# 自定义耗材变量（为空）。
filament_custom_variables = ""
# 耗材密度（g/cm³），0为未设置。
filament_density = 0
# 耗材直径（mm），常见为1.75。
filament_diameter = 1.75
# 换料“蘸料”时的回抽速度（mm/s）。
filament_dip_extraction_speed = 70
# 换料“蘸料”时的插入速度（mm/s）。
filament_dip_insertion_speed = 33
# 换料时是否启用部件风扇。0=不启用。
filament_enable_toolchange_part_fan = 0
# 换料时是否启用特殊温度。0=不启用。
filament_enable_toolchange_temp = 0
# 填充顶部时的出料倍增，100%为标准。
filament_fill_top_flow_ratio = 100%
# 首层挤出流量比例，100%为标准。
filament_first_layer_flow_ratio = 100%
# 换料装入时间（秒），0为自动判定。
filament_load_time = 0
# 换料装入速度（mm/s）。
filament_loading_speed = 28
# 换料开始时的装入速度（mm/s，缓慢起步）。
filament_loading_speed_start = 3
# 换料过程中允许的最大重叠出料比例。
filament_max_overlap = 100%
# 耗材最大打印速度（mm/s），0为不限制。
filament_max_speed = 0
# 耗材最大体积流速（mm³/s），0为不限制。
filament_max_volumetric_speed = 0
# 换色擦拭塔最大速度，0为不限制。
filament_max_wipe_tower_speed = 0
# 换料在加热区的暂停时间（秒）。
filament_melt_zone_pause = 0
# 换色擦拭塔最小清洗量（mm³）。
filament_minimal_purge_on_wipe_tower = 15
# 多工具换料时是否启用ramming（冲料）。0=不启用。
filament_multitool_ramming = 0
# 多工具ramming时的流量（mm³/s）。
filament_multitool_ramming_flow = 10
# 多工具ramming时的体积（mm³）。
filament_multitool_ramming_volume = 10
# 耗材注释说明（为空）。
filament_notes = ""
# 压力提前补偿（单位mm），用于补偿高加速时的出料延迟。!0.02为0.02mm，并可被覆盖。
filament_pressure_advance = !0.02
# 换料冲料（ramming）参数，控制多色打印切换时推料的速度和量（高级耗材管理相关）。
filament_ramming_parameters = "120 100 6.6 6.8 7.2 7.6 7.9 8.2 8.7 9.4 9.9 10.0| 0.05 6.6 0.45 6.8 0.95 7.8 1.45 8.3 1.95 9.7 2.45 10 2.95 7.6 3.45 7.6 3.95 7.6 4.45 7.6 4.95 7.6"
# 耗材收缩率，100%=不补偿，一般用来补偿某些材料冷却收缩。
filament_shrink = 100%
# skinnydip 换料的蘸料距离（mm），多色打印相关。
filament_skinnydip_distance = 31
# 是否为可溶性耗材（0=否，1=是）。
filament_soluble = 0
# 耗材盘重量（克），用于估算剩余耗材量。
filament_spool_weight = 0
# 换色/换料时的延迟（秒）。
filament_toolchange_delay = 0
# 换料时部件风扇速度（百分比）。
filament_toolchange_part_fan_speed = 50
# 换料时的喷嘴温度（℃）。
filament_toolchange_temp = 200
# 当前耗材类型（如PLA、ABS等）。
filament_type = PLA
# 换料卸料时间（秒，0=自动判定）。
filament_unload_time = 0
# 换料卸料速度（mm/s）。
filament_unloading_speed = 90
# 换料卸料起始速度（mm/s）。
filament_unloading_speed_start = 100
# skinnydip蘸料是否使用快速模式（0=否）。
filament_use_fast_skinnydip = 0
# 是否启用 skinnydip 蘸料（0=否）。
filament_use_skinnydip = 0
# 高级擦拭时色料系数（多色打印残留控制）。
filament_wipe_advanced_pigment = 0.5
# 填充层是否齐平对齐（1=对齐，0=不对齐）。
fill_aligned_z = 1
# 填充线条的角度（度）。
fill_angle = 45
### 是否交叉角度填充（1=是）。
fill_angle_cross = 1
# 填充角度是否跟随模型方向（0=否）。
fill_angle_follow_model = 0
# 填充角度递增量（度）。
fill_angle_increment = 0
# 填充角度模板（为空，表示不指定）。
fill_angle_template =
# 填充密度（百分比，18%为较稀疏填充）。
fill_density = 18%
# 填充图案（如 stars 星形、rectilinear 直线等）。
fill_pattern = stars
# 平滑填充分布参数（百分比）。
fill_smooth_distribution = 10%
# 平滑填充宽度（百分比）。
fill_smooth_width = 50%
# 顶部填充的出料倍增比例（100%=标准）。
fill_top_flow_ratio = 100%
# 首层加速度（0=使用默认）。
first_layer_acceleration = 0
# 打底垫(raft)上首层加速度（0=使用默认）。
first_layer_acceleration_over_raft = 0
# 首层热床温度（0=采用默认或不加热）。
first_layer_bed_temperature = 0
# 首层挤出线间距（空=自动）。
first_layer_extrusion_spacing =
# 首层挤出宽度，喷嘴宽度的140%，增大首层附着力。
first_layer_extrusion_width = 140%
# 首层出料流量比例（100%=标准）。
first_layer_flow_ratio = 100%
# 首层高度，喷嘴直径的75%。
first_layer_height = 75%
# 首层填充线间距（空=自动）。
first_layer_infill_extrusion_spacing =
# 首层填充挤出宽度，感叹号表示可被覆盖（140%）。
first_layer_infill_extrusion_width = !140%
# 首层填充速度（mm/s，0=默认）。
first_layer_infill_speed = 0
# 首层最低速度（0=默认）。
first_layer_min_speed = 0
# 首层尺寸补偿（mm）。
first_layer_size_compensation = 0
# 尺寸补偿影响的层数（1层）。
first_layer_size_compensation_layers = 1
# 首层尺寸补偿是否强制不重叠（1=是）。
first_layer_size_compensation_no_collapse = 1
# 首层速度（mm/s）。
first_layer_speed = 30
# 打底垫(raft)上的首层速度。
first_layer_speed_over_raft = 30
# 首层喷嘴温度（℃）。
first_layer_temperature = 200
# 第4层后风扇全速。
full_fan_speed_layer = 4
# 模糊皮肤表面处理（none=关闭）。
fuzzy_skin = none
# 模糊皮肤点间距（百分比）。
fuzzy_skin_point_dist = 200%
# 模糊皮肤厚度（百分比）。
fuzzy_skin_thickness = 150%
# 缝隙填充（Gap fill）时的加速度（0=使用默认/固件值）。
gap_fill_acceleration = 0

### 是否启用缝隙填充（用于较窄区域填充）。1=启用。
gap_fill_enabled = 1
# 缝隙填充线的延伸长度（mm，0=不延伸）。
gap_fill_extension = 0
# 缝隙填充时风扇速度（百分比，带!表示可被其他设置覆盖）。
gap_fill_fan_speed = !100
# 缝隙填充与外壁流量的匹配百分比。
gap_fill_flow_match_perimeter = 0%
# 是否最后打印缝隙填充。0=不强制最后。
gap_fill_last = 0
# 缝隙填充最大宽度（mm，0=默认自动）。
gap_fill_max_width = 0
# 最小缝隙填充区域（百分比，100%为不限制）。
gap_fill_min_area = 100%
# 缝隙填充的最小长度（mm）。
gap_fill_min_length = 0
# 缝隙填充的最小宽度（mm）。
gap_fill_min_width = 0
# 缝隙填充与其他填充/外壁的重叠百分比。
gap_fill_overlap = 80%
gap_fill_perimeter = 1
# 缝隙填充的打印速度，按默认速度的百分比。
gap_fill_speed = 50%
# 是否输出ASCII G-code（0=普通文本格式）。
gcode_ascii = 0
# G-code缓冲区大小，影响一次传输指令数量。
gcode_command_buffer = 10
# 是否在G-code中添加注释。0=不添加。
gcode_comments = 0
# 不允许出现在G-code文件名中的特殊字符列表。
gcode_filename_illegal_char = ... ;[<>:"/\\\\|?*]
# G-code风格（适配目标固件，如Marlin、Reprap等）。
gcode_flavor = marlin
# G-code中对象标签格式，octoprint=适配OctoPrint。
gcode_label_objects = octoprint
# G-code中最小移动距离（mm），低于此值可能被省略。
gcode_min_length = 0.02
# G-code最小分辨率（mm），0=自动。
gcode_min_resolution = 0
# E轴（挤出轴）G-code数值的小数位数。
gcode_precision_e = 5

### XYZ轴G-code数值的小数位数。
gcode_precision_xyz = 3
# G-code中自定义变量替换表（为空）。
gcode_substitutions =
# 换料时是否临时提升电流（0=不提升）。
high_current_on_filament_swap = 0
# 孔径尺寸补偿，0=不补偿。
hole_size_compensation = 0
# 孔径补偿的应用阈值（mm，100=全部应用）。
hole_size_threshold = 100
# 是否将圆孔多边形化（0=不多边形化）。
hole_to_polyhole = 0
# 多边形化孔的最小尺寸变化阈值。
hole_to_polyhole_threshold = 0.01
# 多边形化孔时是否交错多边形顶点（1=是）。
hole_to_polyhole_twisted = 1
# 空闲状态下喷头温度（℃，带!表示可被覆盖）。
idle_temperature = !30
# 填充加速度（0=固件默认）。
infill_acceleration = 0
# 填充锚点长度（百分比，锚点提升附着力）。
infill_anchor = 600%
# 填充锚点最大长度（mm，0=不限制）。
infill_anchor_max = 0
# 填充线是否连接。connected=连接。
infill_connection = connected
# 底部填充线连接方式。connected=连接。
infill_connection_bottom = connected
# 桥接填充线连接方式。notconnected=不连接。
infill_connection_bridge = notconnected
# 实心填充线连接方式。connected=连接。
infill_connection_solid = connected
# 顶部填充线连接方式。connected=连接。
infill_connection_top = connected
# 是否启用密集填充（0=否）。
infill_dense = 0
# 密集填充算法。autoenlarged=自动增大密度。
infill_dense_algo = autoenlarged
# 每隔多少层打印一次填充（1=每层都填充）。
infill_every_layers = 1
# 填充使用的喷头编号（1=第一个喷头）。
infill_extruder = 1
# 奇数层填充是否改变出料量（0=否）。
infill_extrusion_change_odd_layers = 0
# 填充线间距（喷嘴宽度的百分比）。
infill_extrusion_spacing = 100%
# 填充线宽度（空=自动）。
infill_extrusion_width =
# 填充时风扇速度（百分比，!表示可被覆盖）。
infill_fan_speed = !100
# 是否优先打印填充（0=否）。
infill_first = 0
# 填充与外壁重叠百分比。
infill_overlap = 25%
# 填充速度（默认速度的百分比，300%为3倍速度）。
infill_speed = 300%
# 打印前模型初始绕Z轴旋转角度（度）。
init_z_rotate = 0
# 界面壳层数（通常用于多材料/支撑界面）。
interface_shells = 0
# 内部桥接区域的加速度（0=默认/固件值）。
internal_bridge_acceleration = 0
# 内部桥接区域宽度扩展系数（1=标准宽度）。
internal_bridge_expansion = 1
# 内部桥接时风扇速度（百分比，!表示可被别处覆盖）。
internal_bridge_fan_speed = !100
# 内部桥接最小宽度（喷嘴宽度的百分比）。
internal_bridge_min_width = 300%
# 内部桥接速度（默认速度的百分比）。
internal_bridge_speed = 150%
# 是否启用顶面熨平（0=禁用）。
ironing = 0
# 熨平时的加速度（0=默认）。
ironing_acceleration = 0
# 熨平线条角度（度）。
ironing_angle = -45
# 熨平时的出料流量（百分比）。
ironing_flowrate = 15%
# 熨平线间距（喷嘴宽度的百分比）。
ironing_spacing = 25%
# 熨平时速度（默认速度的百分比）。
ironing_speed = 50%
# 熨平类型（top=只对顶面生效）。
ironing_type = top
# 每层开始时插入的G-code（为空）。
layer_gcode =
# 层高（mm）。不能超过喷嘴直径.
layer_height = 0.2
# 回抽抬升的最小高度（mm）。
lift_min = 0
# 设备限值的用途（time_estimate_only=仅用于时间估算，不用于限制实际G-code）。
machine_limits_usage = time_estimate_only
# 挤出轴最大加速度（mm/s²），两个数字分别代表主/次喷头。
machine_max_acceleration_e = 10000,5000
# 挤出时的最大加速度（mm/s²），主/次喷头。
machine_max_acceleration_extruding = 1500,1250
# 回抽时的最大加速度（mm/s²），主/次喷头。
machine_max_acceleration_retracting = 1500,1250
# 空行程最大加速度（mm/s²），主/次喷头。
machine_max_acceleration_travel = 1500,1250
# X轴最大加速度（mm/s²），主/次喷头。
machine_max_acceleration_x = 9000,1000
# Y轴最大加速度（mm/s²），主/次喷头。
machine_max_acceleration_y = 9000,1000
# Z轴最大加速度（mm/s²），主/次喷头。
machine_max_acceleration_z = 500,200
# 挤出轴最大进给速度（mm/s），主/次喷头。
machine_max_feedrate_e = 120,120
# X轴最大进给速度（mm/s），主/次喷头。
machine_max_feedrate_x = 500,200
# Y轴最大进给速度（mm/s），主/次喷头。
machine_max_feedrate_y = 500,200
# Z轴最大进给速度（mm/s），主/次喷头。
machine_max_feedrate_z = 12,12
# 挤出轴最大加加速度（jerk），主/次喷头。
machine_max_jerk_e = 2.5,2.5
# X轴最大jerk，主/次喷头。
machine_max_jerk_x = 10,10
# Y轴最大jerk，主/次喷头。
machine_max_jerk_y = 10,10
# Z轴最大jerk，主/次喷头。
machine_max_jerk_z = 0.2,0.4
# 挤出轴最小进给速度（mm/s），主/次喷头。
machine_min_extruding_rate = 0,0
# 空行程最小进给速度（mm/s），主/次喷头。
machine_min_travel_rate = 0,0
# 风扇最大速度（百分比）。
max_fan_speed = 100
# 每秒输出的最大G-code行数（!表示可被覆盖）。
max_gcode_per_second = !1500
# 最大允许层高（喷嘴直径的百分比）。
max_layer_height = 75%

### 最大允许打印高度（mm）。
max_print_height = 200
# 最大打印速度（mm/s）。
max_print_speed = 80
# 最大速度降低比例（百分比）。
max_speed_reduction = 90%
# 最大体积出料速率变化的负斜率（mm³/s²）。
max_volumetric_extrusion_rate_slope_negative = 0
# 最大体积出料速率变化的正斜率（mm³/s²）。
max_volumetric_extrusion_rate_slope_positive = 0
# 最大允许体积流速（mm³/s，0=不限制）。
max_volumetric_speed = 0
# 铣削后Z轴高度的百分比设定（多功能机/混合加工相关，200%即2倍标准高度）。
milling_after_z = 200%
# 铣刀直径（mm，空为未设置，仅用于支持铣削的设备）。
milling_diameter =
# 铣削区域的额外尺寸（百分比，防止边缘残留）。
milling_extra_size = 150%
# 是否启用铣削后处理。0=不启用。
milling_post_process = 0
# 铣削速度（mm/s）。
milling_speed = 30
# 铣削结束换刀时的G-code（为空，表示未自定义）。
milling_toolchange_end_gcode =
# 铣削开始换刀时的G-code（为空）。
milling_toolchange_start_gcode =
# 铣削抬升高度（mm，空为默认）。
milling_z_lift =
# 最小挤出线宽（喷嘴宽度的百分比）。
min_bead_width = 85%
# 可打印的最小特征尺寸（百分比，低于此值将被忽略或优化）。
min_feature_size = 25%

### 最小层高（喷嘴直径的百分比）。
min_layer_height = 5%
# 最低打印速度限制（mm/s）。
min_print_speed = 10
# skrit（裙边）的最小长度（mm）。
min_skirt_length = 0
# 顶面可打印的最小线宽（喷嘴宽度的百分比）。
min_width_top_surface = 100%
# MMU多色打印时分段互锁深度（mm）。
mmu_segmented_region_interlocking_depth = 0
# MMU多色打印时单段最大宽度（mm）。
mmu_segmented_region_max_width = 0
# 模型处理精度（mm），值越小细节越多。
model_precision = 0.0001
# 外壁不支撑区域的算法（none=不特殊处理）。
no_perimeter_unsupported_algo = none
# 配置说明备注（为空）。
notes =

### 喷嘴直径（mm）。决定最细线条宽度及推荐层高。
nozzle_diameter = 0.4
# 每个对象单独插入的G-code（为空）。
object_gcode =

### 首层是否只有一圈外壁。0=否。
only_one_perimeter_first_layer = 0

### 顶层是否只打印一圈外壁。1=是。
only_one_perimeter_top = 1
# 顶层单圈外壁的其他算法（0=默认）。
only_one_perimeter_top_other_algo = 0
# 仅在穿越外壁时回抽。0=否，1=是。
only_retract_when_crossing_perimeters = 0
# 是否启用防渗料功能（0=否）。
ooze_prevention = 0
# 输出G-code文件的命名格式，{input_filename_base}为模型原文件名。
output_filename_format = {input_filename_base}.gcode
# 桥接区域的出料倍增（百分比）。
over_bridge_flow_ratio = 100%
# 悬垂区域的加速度（0=默认）。
overhangs_acceleration = 0
# 判断为桥接的悬垂阈值（!0=默认并可覆盖）。
overhangs_bridge_threshold = !0
# 桥接上方的实心层数。
overhangs_bridge_upper_layers = 2
# 悬垂处根据倾斜度动态调整风扇转速的设定表。
overhangs_dynamic_fan_speed = !0:5:1:0x100:25x80:50x60:75x40:100x20
# 悬垂处根据倾斜度动态调整打印速度的设定表。
overhangs_dynamic_speed = !0:5:1:0x0:25x10:50x40:75x70:100x100
# 悬垂区域风扇速度（百分比）。
overhangs_fan_speed = !100

### 最陡悬垂坡度（度，0=自动判定）。
overhangs_max_slope = 0
# 是否反向打印悬垂区域（0=否）。
overhangs_reverse = 0
# 反向打印悬垂的阈值（百分比）。
overhangs_reverse_threshold = 250%
# 悬垂区域打印速度（百分比）。
overhangs_speed = 100%
# 是否强制悬垂速度。0=否。
overhangs_speed_enforce = 0
# 悬垂区域线宽（喷嘴宽度的百分比）。
overhangs_width = 75%
# 悬垂区域打印速度（百分比）。
overhangs_width_speed = 55%
# 并行打印多个物体时的步进距离（mm，0=默认）。
parallel_objects_step = 0
# 停靠位置的回抽长度（mm，多材料/多喷头相关）。
parking_pos_retraction = 92
# 打印暂停时插入的G-code（为空）。
pause_print_gcode =
# 外壁打印的加速度（0=使用默认值）。
perimeter_acceleration = 0
# 外壁与填充/内壁的粘结强度参数（百分比）。
perimeter_bonding = 0%
# 外壁打印方向，ccw_cw=外圈逆时针、内圈顺时针。
perimeter_direction = ccw_cw
# 外壁使用的喷头编号（1=第一个喷头）。
perimeter_extruder = 1
# 奇数层外壁是否变更出料量（0=否）。
perimeter_extrusion_change_odd_layers = 0
# 外壁挤出线间距（喷嘴宽度的百分比）。
perimeter_extrusion_spacing = 100%

### 外壁挤出线宽（空=自动）。
perimeter_extrusion_width =
# 外壁打印风扇速度（百分比，带!表示可被覆盖）。
perimeter_fan_speed = !100
# 外壁路径生成算法（classic=传统方式）。
perimeter_generator = classic
# 是否启用外壁回环（0=不启用）。
perimeter_loop = 0
#外壁回环的接缝位置（rear=位于模型后方）。
perimeter_loop_seam = rear
# 外壁与内壁/填充的重叠百分比。
perimeter_overlap = 80%
# 是否反向打印外壁（0=否）。
perimeter_reverse = 0
# 外壁拐角圆角半径（mm）。
perimeter_round_corners = 0
# 外壁打印速度（默认速度的百分比）。
perimeter_speed = 60%

### 外壁圈数（每层3圈）。
perimeters = 3
# 孔洞外壁圈数（!0=默认并可被覆盖）。
perimeters_hole = !0
# 切片后处理脚本（为空）。
post_process =

### 打印前喷嘴就位的坐标（mm）。
priming_position = 0x0
# 打印时热床温度（℃，!60表示可被其他配置覆盖）。
print_bed_temperature = !60
# 打印自定义变量（为空）。
print_custom_variables =
# 打印时全局出料倍增（百分比）。
print_extrusion_multiplier = 100%
# 首层热床温度（℃，!60表示可被覆盖）。
print_first_layer_bed_temperature = !60
# 首层喷嘴温度（℃，!200表示可被覆盖）。
print_first_layer_temperature = !200
# 打印回抽长度（mm，!0表示可被覆盖）。
print_retract_length = !0
# 打印回抽时抬升高度（mm，!0表示可被覆盖）。
print_retract_lift = !0
# 打印时喷嘴温度（℃，!200表示可被覆盖）。
print_temperature = !200
# 打印机自定义变量（为空）。
printer_custom_variables =
# 打印机型号（为空）。
printer_model =
# 打印机备注（为空）。
printer_notes =
# 打印技术类型（FFF=熔融沉积成型）。
printer_technology = FFF

### 打底垫(Raft)与模型的接触距离（mm）。
raft_contact_distance = 0.1
# 打底垫接触距离类型（plane=平面距离）。
raft_contact_distance_type = plane
# 打底垫边缘扩展宽度（mm）。
raft_expansion = 1.5
# 打底垫首层密度（百分比）。
raft_first_layer_density = 90%
# 打底垫首层扩展宽度（mm）。
raft_first_layer_expansion = 3
# 打底垫界面层高度（mm，0=自动）。
raft_interface_layer_height = 0

### 打底垫层高（mm，0=自动）。
raft_layer_height = 0
# 打底垫总层数（0=不生成Raft）。
raft_layers = 0
# 区域专用G-code（为空）。
region_gcode =

### 是否在G-code中输出剩余时间信息（0=否）。
remaining_times = 0
# 剩余时间格式（m73=兼容Prusa/Marlin固件）。
remaining_times_type = m73

### 切片精度（mm），影响G-code点密度和模型还原度。
resolution = 0.0125
# 内部模型处理精度（mm）。
resolution_internal = 0.025
# 空行程前回抽阈值（mm）。
retract_before_travel = 2
# 擦拭前回抽出料百分比。
retract_before_wipe = 0%
# 层切换时是否回抽（0=否）。
retract_layer_change = 0
# 标准回抽长度（mm）。
retract_length = 2
# 换料/换头时的回抽长度（mm）。
retract_length_toolchange = 10
# 回抽时抬升喷头高度（mm）。
retract_lift = 0
# 仅高于此高度时才抬升（mm）。
retract_lift_above = 0
# 空行程前抬升的阈值（mm）。
retract_lift_before_travel = 2
# 仅低于此高度时才抬升（mm）。
retract_lift_below = 0
# 首层回抽时抬升高度（mm）。
retract_lift_first_layer = 0
# 抬升适用表面（All surfaces=所有表面都抬升）。
retract_lift_top = "All surfaces"
# 回抽恢复时额外补出料长度（mm）。
retract_restart_extra = 0
# 换料时回抽恢复补出料长度（mm）。
retract_restart_extra_toolchange = 0
# 回抽速度（mm/s）。
retract_speed = 40
# 接缝角度代价（百分比），用于优化外壁缝隙（seam）位置时的权重。
seam_angle_cost = 60%
# 外壁接缝缝隙大小（百分比）。
seam_gap = 15%
# 外部外壁接缝缝隙（mm）。
seam_gap_external = 0
# 是否对所有接缝进行切口处理。0=否。
seam_notch_all = 0
# 接缝切口的角度（度）。
seam_notch_angle = 250
# 内部接缝是否切口。0=否。
seam_notch_inner = 0
# 外部接缝是否切口。0=否。
seam_notch_outer = 0
# 外壁接缝选择方式（cost=根据代价最小化算法优化接缝位置）。
seam_position = cost
# 接缝移动路径的代价（百分比，用于seam算法）。
seam_travel_cost = 100%
# 是否考虑接缝可见性（1=考虑）。
seam_visibility = 1
# 是否启用静音模式（0=否，1=是）。
silent_mode = 0
# 是否启用单喷头多材料打印（0=否）。
single_extruder_multi_material = 0
# 单喷头多材料打印时是否启用预热/预挤出（1=启用）。
single_extruder_multi_material_priming = 1
# skirt/brim类型选择（0=无，仅skirt或brim分开设定）。
skirt_brim = 0
# skirt（裙边）与模型的距离（mm）。
skirt_distance = 6
# skirt与brim之间的距离（mm）。
skirt_distance_from_brim = 1
# skirt挤出宽度（喷嘴宽度的百分比）。
skirt_extrusion_width = 130%

### skirt高度（层数，1=首层）。
skirt_height = 1

### skirt圈数（1圈）。
skirts = 1
# 切片轮廓闭合的半径阈值（mm），影响边界精度。
slice_closing_radius = 0.049

### 切片模式（regular=常规切片）。
slicing_mode = regular
# 单层打印时间低于此值（秒）时降低打印速度。
slowdown_below_layer_time = 5
# 小面积填充流量补偿模型，不同面积下的流量补偿表。
small_area_infill_flow_compensation_model = !0:10:2:0x0:0.2x0.44:0.4x0.61:0.6x0.7:0.8x0.76:1.5x0.86:2x0.89:3x0.92:5x0.95:10x1
# 判定为小外圈的最大长度（mm）。
small_perimeter_max_length = 20
# 判定为小外圈的最小长度（mm）。
small_perimeter_min_length = 6
# 小外圈打印速度（默认速度的百分比）。
small_perimeter_speed = 50%
# 实心填充图案（rectilineargapfill=直线+补缝）。
solid_fill_pattern = ensuring
# 实心填充加速度（0=默认）。
solid_infill_acceleration = 0
# 小于此面积（mm²）区域强制实心填充。
solid_infill_below_area = 4
# 小于此面积的层强制实心填充（0=不限制）。
solid_infill_below_layer_area = 0
# 小于此宽度的区域强制实心填充（mm，0=不限制）。
solid_infill_below_width = 0

### 每隔多少层打一次实心填充（0=每层都打）。
solid_infill_every_layers = 0
# 实心填充用的喷头编号（1=第一个喷头）。
solid_infill_extruder = 1
# 奇数层实心填充是否变更出料量（0=否）。
solid_infill_extrusion_change_odd_layers = 0
# 实心填充挤出线间距（喷嘴宽度的百分比）。
solid_infill_extrusion_spacing = 100%
# 实心填充线宽（空=自动）。
solid_infill_extrusion_width =
# 实心填充时风扇速度（百分比，!表示可被覆盖）。
solid_infill_fan_speed = !100
# 实心填充与外壁的重叠百分比。
solid_infill_overlap = 100%
# 实心填充打印速度（默认速度的百分比）。
solid_infill_speed = 30%
# 外壁上方是否打印额外实心填充（0=否）。
solid_over_perimeters = 0

### 是否启用螺旋花瓶模式（0=否）。
spiral_vase = 0
# 是否错开内壁接缝（0=否）。
staggered_inner_seams = 0
# 待机喷头温度比主喷头低多少度（℃）。
standby_temperature_delta = -5
# 换料开始时插入的G-code。
start_filament_gcode = "; Filament gcode\n"
# 打印开始时插入的G-code，示例为回零和抬升喷头。
start_gcode = G28 ; home all axes\nG1 Z5 F5000 ; lift nozzle\n
# 是否手动插入start_gcode（0=否）。
start_gcode_manual = 0

### 是否启用支撑结构（0=不启用，1=启用）。
support_material = 0
# 支撑打印时的加速度（0=默认）。
support_material_acceleration = 0
# 支撑结构打印角度（度，0=自动/默认）。
support_material_angle = 0
# 支撑结构不同高度时的角度变化（0=不变化）。
support_material_angle_height = 0

### 是否自动检测需要支撑的区域（1=自动检测）。
support_material_auto = 1
# 支撑底部与模型的接触间距（mm）。
support_material_bottom_contact_distance = 0.2
# 支撑底部界面层数（!0=默认，可被覆盖）。
support_material_bottom_interface_layers = !0
# 支撑底部界面层的填充图案（auto=自动选择）。
support_material_bottom_interface_pattern = auto
# 只在底板上生成支撑（0=否，1=是）。
support_material_buildplate_only = 0
# 支撑顶面闭合结构半径（mm）。
support_material_closing_radius = 2
# 支撑与模型的横向接触距离（mm）。
support_material_contact_distance = 0.2
# 支撑接触距离类型（filament=按耗材直径，plane=按平面距离）。
support_material_contact_distance_type = filament
# 强制支撑的最小层数（0=不强制）。
support_material_enforce_layers = 0
# 支撑使用的喷头编号（0=第一个喷头）。
support_material_extruder = 0
# 支撑挤出宽度（mm，0=自动）。
support_material_extrusion_width = 0
# 支撑打印时风扇速度（百分比，!表示可覆盖）。
support_material_fan_speed = !100
# 支撑界面层加速度（0=默认）。
support_material_interface_acceleration = 0
# 支撑界面层填充角度（度）。
support_material_interface_angle = 90
# 支撑界面层填充角度递增量（度）。
support_material_interface_angle_increment = 0
# 支撑界面层是否生成接触环（0=否）。
support_material_interface_contact_loops = 0
# 支撑界面层用的喷头编号（0=第一个喷头）。
support_material_interface_extruder = 0
# 支撑界面层风扇速度（百分比，!表示可覆盖）。
support_material_interface_fan_speed = !100
# 支撑界面层层高（mm，0=默认）。
support_material_interface_layer_height = 0
# 支撑界面层数（3层）。
support_material_interface_layers = 3
# 支撑界面层线间距（mm，0=默认）。
support_material_interface_spacing = 0
# 支撑界面层速度（默认速度的百分比）。
support_material_interface_speed = 50%
# 支撑层高（mm，0=默认/跟随主层高）。
support_material_layer_height = 0
# 支撑图案（rectilinear=直线）。
support_material_pattern = rectilinear
# 支撑线间距（mm）。
support_material_spacing = 2.5
# 支撑打印速度（默认速度的百分比）。
support_material_speed = 60%
# 支撑风格（grid=方格）。
support_material_style = grid
# 支撑是否与主模型同步层高（0=否）。
support_material_synchronize_layers = 0
# 支撑角度阈值（度，0=默认/自动）。
support_material_threshold = 0
# 支撑顶部界面层的图案（rectilinear=直线）。
support_material_top_interface_pattern = rectilinear
# 支撑外包壳（1=生成外壳使支撑易拆）。
support_material_with_sheath = 1
# 支撑与模型在XY平面的间隙（百分比）。
support_material_xy_spacing = 50%
# 树状支撑分支最大角度（度）。
support_tree_angle = 40
# 树状支撑慢速分支最大角度（度）。
support_tree_angle_slow = 25
# 树状支撑分支直径（mm）。
support_tree_branch_diameter = 2
# 树状支撑分支直径增长角度（度）。
support_tree_branch_diameter_angle = 5
# 树状支撑双壁分支直径（mm）。
support_tree_branch_diameter_double_wall = 3
# 树状支撑分支间最小距离（mm）。
support_tree_branch_distance = 1
# 树状支撑最顶端直径（mm）。
support_tree_tip_diameter = 0.8
# 树状支撑顶端收束率（百分比）。
support_tree_top_rate = 15%
# 喷嘴打印温度（℃）。
temperature = 200
# 模板自定义G-code（为空）。
template_custom_gcode =
# 薄壁打印的线宽（喷嘴宽度百分比）。
thin_perimeters = 80%
# 所有薄壁区域允许的最小线宽（百分比）。
thin_perimeters_all = 20%

### 是否允许薄墙打印（1=允许）。
thin_walls = 1
# 薄壁打印的加速度（0=默认）。
thin_walls_acceleration = 0
# 是否合并多条薄壁（1=合并）。
thin_walls_merge = 1
# 薄壁允许的最小宽度（喷嘴宽度百分比）。
thin_walls_min_width = 33%
# 薄壁线之间的重叠度（百分比）。
thin_walls_overlap = 50%
# 薄壁打印速度（默认速度百分比）。
thin_walls_speed = 100%

### G-code内嵌缩略图尺寸（px，0x0为不生成）。
thumbnails = 0x0,0x0
# 缩略图主色（HEX值）。
thumbnails_color = #018aff
# 是否自定义缩略图颜色（0=否）。
thumbnails_custom_color = 0
# 缩略图是否在文件结尾（0=否）。
thumbnails_end_file = 0
# 缩略图格式（PNG 图片）。
thumbnails_format = PNG
# 缩略图标签格式（0=默认）。
thumbnails_tag_format = 0
# 缩略图是否包含打印床（1=包含）。
thumbnails_with_bed = 1
# 打印时间估算的附加时间（秒），补偿切片器估算与实际耗时的差异。
time_cost = 0
# 时间估算补偿系数（百分比），100%表示不调整。
time_estimation_compensation = 100%
# 执行起始G-code所需的时间（秒），用于整体时间估算。
time_start_gcode = 20
# 多喷头/多材料换料或切换时间（秒），用于时间估算。
time_toolchange = 30
# 工具（喷头）名称（为空）。
tool_name = ""
# 切换喷头/材料时插入的G-code（为空）。
toolchange_gcode =
# 顶部区域风扇速度（百分比，!表示此处可被其他设置覆盖）。
top_fan_speed = !100
# 顶面填充图案（monotonic=单向顺序填充）。
top_fill_pattern = monotonic
# 顶面填充线间距（空=自动）。
top_infill_extrusion_spacing =
# 顶面填充线宽（喷嘴宽度的105%）。
top_infill_extrusion_width = 105%
# 顶面实心填充加速度（0=默认）。
top_solid_infill_acceleration = 0
# 顶面实心填充与外壁的重叠比例（百分比）。
top_solid_infill_overlap = 100%
# 顶面实心填充速度（默认速度的百分比）。
top_solid_infill_speed = 50%

### 顶面实心层数（3层）。
top_solid_layers = 3
# 顶面实心总最小厚度（mm，0=仅按层数决定）。
top_solid_min_thickness = 0
# 空行程加速度（mm/s²）。
travel_acceleration = 1500
# 空行程减速是否按目标速度计算（1=是）。
travel_deceleration_use_target = 1
# 碰撞障碍前是否抬升喷头（0=否）。
travel_lift_before_obstacle = 0
# 空行程斜坡抬升高度（mm，0=不抬升）。
travel_ramping_lift = 0
# 空行程路径允许的最大坡度（0=不限制）。
travel_slope = 0
# 空行程速度（mm/s）。
travel_speed = 130
# Z轴空行程速度（mm/s，0=默认/固件值）。
travel_speed_z = 0
# 是否使用固件回抽（0=G-code直接控制，1=G10/G11固件控制）。
use_firmware_retraction = 0
# 挤出量E轴是否使用相对模式（0=绝对，1=相对）。
use_relative_e_distances = 0
# 是否启用体积出料模式（0=否，1=是）。
use_volumetric_e = 0
# 是否启用可变层高（1=启用，0=禁用）。
variable_layer_height = 1
# 外壁厚度分布的计算次数（高级外壁优化相关）。
wall_distribution_count = 1
# 外壁过渡的临界角度（度），小于该角度时进行过渡优化。
wall_transition_angle = 10
# 外壁过渡滤波允许的最大偏差（百分比）。
wall_transition_filter_deviation = 25%
# 外壁过渡长度（百分比）。
wall_transition_length = 100%

### 是否启用打印擦拭（0=否，1=是）。
wipe = 0
# 是否启用高级擦拭（0=否）。
wipe_advanced = 0
# 高级擦拭算法类型（linear=线性，其他如curve等）。
wipe_advanced_algo = linear
# 高级擦拭的倍增系数（用于控制擦拭量）。
wipe_advanced_multiplier = 60
# 高级擦拭喷嘴熔融区体积（mm³）。
wipe_advanced_nozzle_melted_volume = 120
# 擦拭时是否多打一圈外壁（0=否）。
wipe_extra_perimeter = 0
# 内部擦拭深度（百分比）。
wipe_inside_depth = 50%
# 内部擦拭是否在路径末端（1=是）。
wipe_inside_end = 1
# 内部擦拭是否在路径起点（0=否）。
wipe_inside_start = 0
# 是否在填充区域内擦拭（0=否）。
wipe_into_infill = 0
# 是否在物体内部擦拭（0=否）。
wipe_into_objects = 0
# 擦拭时抬升喷头高度（mm，0=不抬升）。
wipe_lift = 0
# 擦拭时抬升的路径长度（百分比）。
wipe_lift_length = 50%
# 最小擦拭长度（百分比）。
wipe_min = 150%
# 仅在穿越（交叉）时擦拭（1=是）。
wipe_only_crossing = 1
# 擦拭速度（mm/s，0=默认）。
wipe_speed = 0
# 是否启用擦拭塔（0=否，多材料相关）。
wipe_tower = 0
# 擦拭塔桥接层数（10层）。
wipe_tower_bridging = 10
# 擦拭塔裙边宽度（mm）。
wipe_tower_brim_width = 2
# 擦拭塔锥度角（度）。
wipe_tower_cone_angle = 0
# 擦拭塔与主模型的额外间距（百分比）。
wipe_tower_extra_spacing = 100%
# 擦拭塔使用的喷头编号（0=第一个喷头）。
wipe_tower_extruder = 0
# 擦拭塔挤出线宽（喷嘴宽度的百分比）。
wipe_tower_extrusion_width = 150%
# 擦拭塔全部为实心层（0=否，允许稀疏层减少耗材）。
wipe_tower_no_sparse_layers = 0
# 擦拭塔旋转角度（度）。
wipe_tower_rotation_angle = 0
# 擦拭塔打印速度（mm/s）。
wipe_tower_speed = 80
# 擦拭塔宽度（mm）。
wipe_tower_width = 60
# 擦拭塔擦拭起始速度（百分比）。
wipe_tower_wipe_starting_speed = 33%
# 擦拭塔X坐标（mm）。
wipe_tower_x = 180
# 擦拭塔Y坐标（mm）。
wipe_tower_y = 140
# 多喷头切换时每个喷头的擦拭体积（mm³）。
wiping_volumes_extruders = 70,70,70,70,70,70,70,70,70,70
# 多喷头间切换时的擦拭体积矩阵（mm³，行列对应不同喷头组合）。
wiping_volumes_matrix = 0,140,140,140,140,140,0,140,140,140,140,140,0,140,140,140,140,140,0,140,140,140,140,140,0
# 内部XY尺寸补偿（mm）。
xy_inner_size_compensation = 0
# 整体XY尺寸补偿（mm）。
xy_size_compensation = 0

### 打印整体Z轴偏移（mm）。
z_offset = 0
# Z轴步进（mm，0=默认单层步进）。
z_step = 0